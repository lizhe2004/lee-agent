[project]
name = "support-agent"
version = "0.1.0"
description = "A workflow-guided customer support CLI agent"
requires-python = ">=3.11"
dependencies = ["jsonschema>=4.22"]

[dependency-groups]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
